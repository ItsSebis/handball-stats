# Handball Stats

Mobile-first handball statistics tracker. See `docs/` for the full spec (data model, architecture, features, roadmap) and `CLAUDE.md` for how to work in this repo.

```bash
npm run dev
```
